# Golang知识点

```
  1. 与nil能进行比较数据类型
      slice,map,interface,chan
      
  2. 不可寻址的变量
      map的值，常量
      
  3. := 短变量的注意事项
     3.1 结构体的字段不能使用短变量 stu.name,age := "haoge",27
   
  4. defer
  
  5. 指针不支持索引计算
      
  
  6. 多项赋值
     i,s[i] := num+1,num+2
        6.1 赋值顺序，等号左边计算->等号右边计算->赋值
     
  7. i++
      7.1 go语言不存在 ++i,--i
      7.2 i++,i--是语句，不是表达式，不能进行赋值操作，如 age := i++
      
  8. 变量隐藏
      8.1 由于作用于不同造成了变量隐藏现象
      
  9. 常量
      9.1 iota只能使用在const代码块中的常量定义，用于标定常量块中的常量行索引
      9.2 常量可以定义，但是不适用
      
  10. 定义不适用，不会报错的情况
      全局变量，常量，函数参数
  
  11. cap()函数使用
      cap不能用于map类型
      
  12. 
```
